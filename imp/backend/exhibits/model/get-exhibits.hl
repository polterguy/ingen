
/*
 * Returns all exhibits in system to caller.
 */
p5.mysql.connect:[ingen]

  /*
   * Selecting all exhibits, and return name and id to caller.
   */
  p5.mysql.select:select * from exhibits
  for-each:x:/-/*
    eval-x:x:/+/*/*/*
    add:x:/..p5.mysql.connect/*/return
      src
        exhibit
          name:x:/@_dp/#/*/name?value
          id:x:/@_dp/#/*/id?value
  return
