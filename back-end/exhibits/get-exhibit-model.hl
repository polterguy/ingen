
/*
 * File responsible for retrieving an exhibit item from database.
 */


/*
 * Sanity checking invocation.
 */
micro.lambda.contract.min:x:/..
  id:long


/*
 * Opening connection, seleting item, and returns values to caller.
 */
p5.mysql.connect:[ingen]
  p5.mysql.select:select * from exhibits where id = @id
    @id:x:/../*/id?value
  return:x:/-/*/*
